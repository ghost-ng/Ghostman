# Ghostman Requirements
# Complete dependency list for Ghostman with FAISS-only RAG pipeline

# Core GUI and application dependencies
PyQt6>=6.4.0
cryptography>=3.4.8

# Database dependencies
sqlalchemy>=2.0.0
alembic>=1.12.0
bleach>=6.0.0

# Core AI service dependencies (unified session manager)
requests>=2.28.0
urllib3>=1.26.0
openai>=1.0.0

# FAISS-only RAG Pipeline
faiss-cpu>=1.7.0  # Primary vector store
numpy<2.0.0  # Compatibility constraint
scipy>=1.7.0
scikit-learn>=1.0.0

# Document processing and loaders
pypdf>=3.17.0  # Modern PyPDF2 replacement
pdfplumber>=0.10.0  # High-quality PDF text extraction with layout
python-docx>=0.8.11
beautifulsoup4>=4.12.0
ddgs>=7.0.0  # DuckDuckGo web search for AI agent tool calling
lxml>=4.9.0
html2text>=2020.1.16
pandas>=2.0.0
openpyxl>=3.1.0

# Text processing and tokenization
tiktoken>=0.5.0
nltk>=3.8.0
chardet>=5.0.0
markdownify>=0.11.0

# File type detection (Windows compatible)
python-magic>=0.4.27
python-magic-bin>=0.4.14

# Async support
aiofiles>=23.0.0
asyncio>=3.4.3

# Caching and performance
diskcache>=5.6.0

# Skills system dependencies
pywin32>=306  # Windows COM automation (Outlook, Windows Search)
pillow>=10.0.0  # Image capture and processing
pytesseract>=0.3.10  # Optional OCR for screen capture
mammoth>=1.6.0  # DOCX to HTML conversion for document preview

# Framework compatibility
pydantic>=2.0.0,<3.0.0

# Syntax highlighting and rendering
mistune==3.1.2
pygments>=2.15.0

# Development and testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-qt>=4.2.0

# Optional dependencies for enhanced functionality
# anthropic>=0.7.0  # For native Anthropic API support
# httpx>=0.24.0     # Alternative HTTP client